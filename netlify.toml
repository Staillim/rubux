[build]
  command = "npm ci --prefix client --include=dev && npm run build --prefix client"
  publish = "client/dist"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
